# Local Slack configuration for interface-reconstruction
# Prefer bot token + channel for file uploads.
SLACK_BOT_TOKEN=xoxb-REPLACE_ME
SLACK_CHANNEL=interface-reconstruction
# Auto-enable Slack notifications in sweep scripts.
SLACK_NOTIFY=1

# Or use a webhook (message only):
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/REPLACE/ME
